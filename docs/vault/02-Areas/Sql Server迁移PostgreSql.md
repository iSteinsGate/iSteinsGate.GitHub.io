
## top
 limit  
## stuff
string_agg
## 类型转换
- 条件参数  where a=3  ==>  where a='3'
- 连接类型表不一致
## 表字段大写
## ISNULL
coalesce
## getdate()
CURRENT_DATE
## DATEIFF

DATEDIFF(DAY
DATE_PART('day'

DATEDIFF(HOUR
DATE_PART('hour'
# IIF
iif前后数据类型必须一致

# CAST(0 AS BOOLEAN)
0::SMALLINT  1::SMALLINT
