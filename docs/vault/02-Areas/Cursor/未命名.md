

  

- 必须使用PostgreSQL14.2兼容语法

- 如果是视图使用`CREATE OR REPLACE VIEW`

- 支持CTE（WITH子句）

- 使用`::`进行类型转换

- 使用`COALESCE`处理NULL

- 使用`ILIKE`进行不区分大小写的匹配

- 窗口函数必须指定`OVER()`子句

- 使用`STRING_AGG`或`ARRAY_AGG`进行聚合