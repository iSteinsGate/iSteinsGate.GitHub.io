


从服务器上下载`filename`到 本机 `/tmp/local_destination`目录下

`scp -P 6720 username@servername:/path/filename /tmp/local_destination`

将本机`local_filename`文件上传到服务起path目录下

`scp -P 6720 /path/local_filename username@servername:/path`